[package]
name = "agentbrowser-macos-bridge"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
agentbrowser-connection = { path = "../../../packages/client-connection" }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time"] }
